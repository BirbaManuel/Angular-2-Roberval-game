# Angular-2-Roberval-game
The Roberval game based on Angular 2

First run the server:
in this path: pathToMyapp/myapp/server
run this command:
npm start

After run the client:
in this path: pathToMyapp/myapp/server
run this command:
npm start pathToMyapp/myapp/
